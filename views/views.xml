<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="board_of_ideas.list">
      <field name="name">Board_of_ideas list</field>
      <field name="model">board_of_ideas.board_of_ideas</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="board_of_ideas.action_window">
      <field name="name">Board_of_ideas window</field>
      <field name="res_model">board_of_ideas.board_of_ideas</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="board_of_ideas.action_server">
      <field name="name">Board_of_ideas server</field>
      <field name="model_id" ref="model_board_of_ideas_board_of_ideas"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="Board_of_ideas" id="board_of_ideas.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="board_of_ideas.menu_1" parent="board_of_ideas.menu_root"/>
    <menuitem name="Menu 2" id="board_of_ideas.menu_2" parent="board_of_ideas.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="board_of_ideas.menu_1_list" parent="board_of_ideas.menu_1"
              action="board_of_ideas.action_window"/>
    <menuitem name="Server to list" id="board_of_ideas" parent="board_of_ideas.menu_2"
              action="board_of_ideas.action_server"/>
    -->
  </data>
</odoo>